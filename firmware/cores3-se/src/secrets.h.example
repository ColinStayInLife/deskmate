/*
 * secrets.h.example — 敏感配置模板
 *
 * 复制为 secrets.h 并填入真实值。
 * secrets.h 已在 .gitignore 中，不会提交到版本管理。
 */

#ifndef SECRETS_H
#define SECRETS_H

// ── WiFi ────────────────────────────────────────────────────────
#define WIFI_SSID           "YourWiFiSSID"
#define WIFI_PASSWORD       "YourWiFiPassword"

// ── WebSocket (ClawCorp Agent) ─────────────────────────────────
#define WS_SERVER           "192.168.1.100"
#define WS_PORT             8765
#define WS_PATH             "/ws/deskmate"

// ── DeepSeek API (通过 ClawCorp 代理) ──────────────────────────
#define DEEPSEEK_API_KEY    "sk-your-deepseek-api-key"
#define DEEPSEEK_MODEL      "deepseek-chat"

#endif // SECRETS_H
